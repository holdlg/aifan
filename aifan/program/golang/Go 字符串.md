# go 字符串

```go
// 大小写
strings.ToLower("str")
strings.ToUpper("str")

// 分割
strings.Split("str#aaa", "#")

// 使用 Printf() 函数 格式化，输出至控制台
fmt.Printf("%s is %d years old.\n", name, age) 

// 使用 Sprintf() 函数 格式化，返回字符串
str := fmt.Sprintf("%s is %d years old.", name, age) 
fmt.Println(str)

```
